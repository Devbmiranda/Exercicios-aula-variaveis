// Coeficientes da equação do segundo grau
let a = 1; // Substitua pelo coeficiente 'a' da sua equação
let b = -3; // Substitua pelo coeficiente 'b' da sua equação
let c = 2; // Substitua pelo coeficiente 'c' da sua equação

// Cálculo do delta
let delta = Math.pow(b, 2) - (4 * a * c);

// Exibindo o resultado no console
console.log(`Coeficientes da equação: a=${a}, b=${b}, c=${c}`);
console.log(`Delta (Δ): ${delta}`);